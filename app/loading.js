import { LoadingOverlay } from "@/components/LoadingOverlay";

// Shown automatically by Next.js while a server component / its data loads.
export default function Loading() {
  return <LoadingOverlay />;
}
